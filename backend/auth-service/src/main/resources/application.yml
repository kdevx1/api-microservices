server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:55433/authdb
    username: devx
    password: devx

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: auth-service


  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB